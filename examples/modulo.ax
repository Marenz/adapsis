+fn is_even (x:Int)->Bool
  +let remainder:Int = x % 2
  +return remainder == 0
+end

+test is_even
  +with 4 -> expect true
  +with 5 -> expect false
  +with 0 -> expect true
  +with -2 -> expect true
  +with 7 -> expect false
